server:
  port: 8002
  servlet:
    context-path: /search

spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: nacos-search-provider

management:
  endpoints:
    web:
      exposure:
        include: "*"